<h2>Forecast for {{ city }}</h2>
<div class="timezone-note">Note: Forecasts are given in the local timezone of the city.</div>
<div class="'local-forecast">
  {% if forecast is not empty%}
    <table>
      <tr>
        <td><strong>Date</strong></td>
        <td><strong>Time</strong></td>
        <td><strong>Forecast</strong></td>
        <td><strong>Temperature</strong></td>
        <td><strong>Humidity</strong></td>
      </tr>
      {%  for day in forecast %}
      <tr>
        <td>{{ day.dt | date("d/m/Y", timezone) }}</td>
        <td>{{ day.dt | date("ga", timezone) }}</td>
        <td>{{ day.weather.0.description | capitalize }} <img src="http://openweathermap.org/img/wn/{{ day.weather.0.icon }}@2x.png" alt="" width="20"></td>
        <td>{{ day.main.temp  }}°C </td>
        <td>{{ day.main.humidity }} %</td>
      </tr>
      {% endfor %}
    </table>
    </li>
  </ul>
  {% else %}
    <div> No data found </div>
  {% endif %}
</div>
